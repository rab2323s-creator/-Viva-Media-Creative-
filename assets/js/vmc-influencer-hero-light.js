 (function(){const hero=document.getElementById('vmcHero');const phone=document.getElementById('vmcPhone');const slides=[...document.querySelectorAll('.slide')];const dots=[...document.querySelectorAll('.dots button')];const faces=[...document.querySelectorAll('.face')];let current=0,timer;function show(i){current=(i+slides.length)%slides.length;slides.forEach((s,n)=>s.classList.toggle('active',n===current));dots.forEach((d,n)=>d.classList.toggle('active',n===current))}function run(){clearInterval(timer);timer=setInterval(()=>show(current+1),4300)}dots.forEach(d=>d.addEventListener('click',()=>{show(+d.dataset.slide||0);run()}));show(0);run();if(hero&&phone&&matchMedia('(pointer:fine)').matches){hero.addEventListener('mousemove',e=>{const r=hero.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;phone.style.transform=`rotateY(${x*7}deg) rotateX(${-y*5}deg)`;faces.forEach((f,i)=>{const depth=(i%4+1)*5;f.style.setProperty('--px',`${x*depth}px`);f.style.setProperty('--py',`${y*depth}px`)})});hero.addEventListener('mouseleave',()=>{phone.style.transform='';faces.forEach(f=>{f.style.setProperty('--px','0px');f.style.setProperty('--py','0px')})})}})();
